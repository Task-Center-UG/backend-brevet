name: Deploy Backend Brevet

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          file: ./DockerFile
          push: true
          tags: zidanindratama/backend-brevet:latest

      - name: Deploy to VPS
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          port: ${{ secrets.VPS_PORT }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.VPS_PRIVATE_KEY }}
          script: |
            set -e
            cd /opt/brevet/backend-brevet
            git pull origin main
            docker compose -f docker-compose.prod.yml pull db redis api migrate
            docker compose -f docker-compose.prod.yml up -d db redis
            docker compose -f docker-compose.prod.yml up --force-recreate migrate
            docker compose -f docker-compose.prod.yml up -d --force-recreate api
            docker image prune -f
